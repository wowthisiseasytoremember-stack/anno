# Handoff — Anno visual-polish: Release Artifacts Authored

**Project:** Anno (Catholic daily devotional iOS app)
**Branch:** visual-polish
**Date:** 2026-08-28
**Session:** resumed from `20260828_053439_894d1f` (plan-only handoff)

---

## What Changed This Session

The prior handoff left the v6 build plan **authored but not executed**. This session executed
Phases 1–3 (all Linux-doable artifacts). Two delegate workers were dispatched but both hit an
HTTP 429 rate-limit on the free model proxy and wrote **nothing** (verified: 0/24 files landed).
Per delegation doctrine (never trust subagent self-reports), fell back to direct authoring.

### Authored (24 files)
**Release docs (`docs/`):**
- `SECRETS.md` — placeholder → GitHub Secret → source mapping table (7 rows)
- `MATCH_SETUP.md` — Mac cert-generation playbook
- `RELEASE_CHECKLIST.md` — 30-min Mac session + hard VI signoff gate
- `VIETNAMESE_REVIEW_SIGNOFF.md` — `vi_review_complete: false` template (release requires `true`)

**StoreKit layer (`storekit-core/`, Linux-testable, NO external imports):**
- `Sources/StoreKitProvider.swift` — protocol + local domain types (StoreOffering/Package/CustomerInfo/Entitlement)
- `Sources/StoreService.swift` — logic-only wrapper (no SwiftUI)
- `Sources/MockStoreProvider.swift` — deterministic in-memory test impl
- `Sources/EntitlementManager.swift` — pure-Swift tier mapping
- `Tests/StoreKitProviderTests.swift` — 6 XCTest cases
- `Package.swift` — `AnnoStoreKit` module, 5.10, no external deps

**App-only adapters (macOS/iOS — import frameworks, NOT in Package.swift):**
- `Anno/Services/RevenueCatProvider.swift` — RevenueCat-backed impl
- `Anno/ViewModels/StoreViewModel.swift` — `@Observable` SwiftUI VM

**Fastlane (`fastlane/`):**
- `Fastfile` — `beta_validate` (no pilot), `beta_deploy` (pilot skip_submission), guarded `match_*` write lanes
- `Appfile`, `Matchfile` (readonly true), `Gemfile` (~> 2.225), `Gemfile.lock` (best-effort pin — regenerate on Mac via `bundle lock`)

**CI:** `.github/workflows/ios-release.yml` — tag-gated (dryrun first, semver second), `release` + `test:storekit` jobs, concurrency group.

**Metadata (`fastlane/metadata/`):** en-US description/keywords/release_notes/support/marketing + vi/README.md gate.

---

## Verification Done
- ✅ All 24 files exist on disk.
- ✅ Linux subset (`storekit-core/`, `Package.swift`) has **zero** `import StoreKit/UIKit/SwiftUI/RevenueCat`.
- ✅ `ios-release.yml` parses as valid YAML (jobs: release, test-storekit).
- ✅ `Fastfile` passes `ruby -c` syntax check.
- ⚠️ **NOT verified:** actual Swift compilation / `swift test --filter StoreKitProviderTests`.
  No Swift toolchain on ichabod. The CI `test:storekit` job (macOS runner) owns this gate.
- ⚠️ `Gemfile.lock` is a best-effort hand pin, NOT generated by `bundle lock`. Regenerate on Mac.

---

## Git State
Working tree dirty (24 new untracked files). NOT committed yet — left for review/commit decision.

---

## Next Steps
1. **Review** the new files; `git add` + commit on `visual-polish` when satisfied.
2. **Compile gate (real):** on a Mac or via CI — `swift test --filter StoreKitProviderTests`
   (uses root `Package.swift`). Fix any Swift errors there.
3. **Dry-run tag:** `git tag v0.0.0-dryrun && git push origin v0.0.0-dryrun` → verify `beta_validate`
   runs, NO TestFlight upload.
4. **Mac session (Option C in prior handoff):** run `MATCH_SETUP.md` → set `vi_review_complete: true`
   in `VIETNAMESE_REVIEW_SIGNOFF.md` → `fastlane beta_deploy`.

---

## Context to Preserve
- Branch: `visual-polish`. No Mac needed for authoring — only `match` init + final `beta_deploy`.
- RevenueCat: `anno.subscription.yearly` ($19.99/yr, 7-day trial), `anno.subscription.monthly` ($2.99/mo); entitlement `premium`.
- VI signoff is a HARD gate before any TestFlight build.
- Workers failed on 429, not on packet defects — re-delegation was skipped because the failure was infra-wide (free proxy throttled), not per-packet.

*GCU No Trouble At All — Mountains move without ceremony*
