# Anno — SESSION HANDOFF (visual polish only)

**Written:** 2026-08-27 21:50 UTC
**Project:** /home/ichabod/Projects/Anno (Native SwiftUI iOS Catholic devotional app — "This day in Catholic history")
**Repo:** github.com/wowthisiseasytoremember-stack/anno (PUBLIC) — branch `main`, git clean.
**Next agent's ONE job:** VISUAL POLISH. Nothing else.

---

## Scope lock (READ THIS FIRST)

You are a **visual / front-end design agent**. Your mandate is narrow and explicit:

> Find everything in the iOS app that is default, generic, or placeholder-looking, and make it **INTENTIONAL** — not busy. Elevate it to the established Anno design language. Restraint over ornament. A screen should feel composed, not decorated.

**You MUST NOT:**
- Change any data, logic, models, fixtures, or pipeline code. (Engine A/B, `data/`, `tools/`, `Anno/Models/`, `Anno/Services/` data loaders — hands off.)
- Run any LLM research, content generation, or new content batch.
- Touch `calendar_engine.py`, `batch_engine_b_2027_gap.py`, `normalize_fixture.py`, validators.
- Add features, new screens, or new user flows.
- Modify secrets, build signing, or the `gh-pages` privacy page.

**You MAY:**
- Edit SwiftUI views, modifiers, themes, typography, colors, spacing, layout, motion, icons, asset catalogs.
- Improve `Anno/Design/`, `Anno/Components/`, any `*.swift` view under `Anno/Today`, `Anno/Calendar`, `Anno/Map`, `Anno/Saved`, `Anno/Settings`, `Anno/Paywall`, `Anno/Reliquary`, `Anno/Onboarding` (if present), `Anno/RootView`, `Anno/AppRouter`.
- Add/replace image assets, SF Symbols usage, gradients, shadows, haptics tied to existing interactions.
- Fix layout bugs, overflow, misalignment, empty/placeholder states that look unfinished.
- Read design refs (below) and align every change to them.

If you find a NON-visual defect that blocks polish (e.g. a data binding is wrong so a view can't render), **note it in your closeout — do not fix it yourself**; route it back.

---

## Established design language (READ BEFORE TOUCHING ANYTHING)

These are the source of truth for the visual language. Do not invent a new aesthetic.

- `Anno/Design/AnnoTheme.swift` — ecclesial palette: `narthex` (near-black warm), `choir`, `goldLeaf`/`gilt` (gold), `lapis`, `crimson`, `vellum`, `incense`, `ash`, `verdigris`, `advent`, `easter`, `rose`, `candleGlow`. Confidence colors (confirmed=told, traditional=lapis, disputed=crimson, contextual=incense). Liturgical color resolvers.
- `Anno/Design/LiturgicalThemeModifier.swift`, `Anno/Design/Typography.swift` — theme + type scale.
- `docs/DESIGN_AGENT_BRIEF.md` — full screen inventory (Onboarding, Today, Calendar, Map, Saved, Settings, Paywall, Reliquary), what each shows, the intended feel. **Start here.**
- `docs/DESIGN_AGENT_BRIEF_VI.md` — Vietnamese-localized brief (app is bilingual EN/VI; respect both).
- `docs/APP_ICON_DESIGN_PLAN.md`, `docs/BRAND_VISUAL_ADDENDUM.md` — icon + brand direction.
- `docs/LITURGICAL_SOLAR_COMPASS_DESIGN.md` — the compass dial design spec (a signature element; implement per spec, don't generic it).

Existing signature components already built (use / extend, don't reinvent):
`Anno/Components/GlassCard.swift`, `TactileDateWheel.swift`, `SacredArtCanvas.swift`, `ShareCard.swift`, `ShareableImage.swift`, `VerseActionBar.swift`, `ConfidenceBadge.swift`, `SourceSheet.swift`, `SourceRow.swift`.

---

## Current state (so you don't re-do finished work)

- **Content/data:** COMPLETE for Jan 2027 (31/31 days, 100% bilingual EN/VI, 0 dead sources). 2026 fixture = 182 days. Both normalized + gate-clean. Do not touch.
- **Engine A:** fixed + regression-tested. Engine B: self-run complete through Jan, driver hardened.
- **Privacy URL:** live (GitHub Pages). TestFlight still Mac-gated (you cannot build on ichabod — no Xcode).
- **What is likely still DEFAULT/GENERIC (your hunt list):** system-default list rows, plain `Text()` with no type style, unstyled `NavigationView`/toolbars, placeholder empty-states ("No results", "Coming soon"), default `SF Symbol` choices, missing haptics on tappable cards, inconsistent spacing, any view not using `GlassCard`/`LiturgicalThemeModifier`, onboarding/paywall if they look stock. Verify against the brief.

---

## How to work

1. Read `docs/DESIGN_AGENT_BRIEF.md` + `Anno/Design/AnnoTheme.swift` fully first.
2. Build a short visual audit: list each screen/view, mark DEFAULT vs INTENTIONAL, pick the top N (start with Today + Calendar + a payment/empty-state) to elevate.
3. Make changes in small, reviewable commits. Prefer extending `AnnoTheme`/`LiturgicalThemeModifier`/`Typography` over hard-coding colors per-view.
4. You cannot compile (no macOS). So: validate Swift syntax by eye + `swiftlint` if available; do NOT claim "builds." State clearly what is unverified.
5. Keep the Vietnamese layout in mind — Vietnamese text is longer; ensure no clipping/overflow in intentional layouts.

---

## Definition of done (for THIS agent)

- Every primary screen reads as intentional and composed, aligned to `AnnoTheme`.
- No obvious system-default rows, plain unstyled text, or "TODO/Coming soon" placeholders left visible.
- Changes committed in small atomic commits with clear messages; no logic/data regressions.
- A closeout written to `.handoff/` noting what was polished, what could not be verified (build), and any non-visual defects found-but-not-fixed.

## Resume command for the orchestrator
`/handoff --read` loads this. If you get stuck, write your own `.handoff/LATEST.md` and stop.
