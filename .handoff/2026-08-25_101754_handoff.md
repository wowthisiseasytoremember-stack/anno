---
mode: pause
session_id: 20260825_090300_opencode
project: anno
branch: main
head_commit: 227bb27
previous_handoff: none
ts: 2026-08-25T10:17:54Z
work: Audited + incorporated three other agents' drops (surfacebook pilgrimage audio/icons, ReliquaryExplorer AR app, Vietnamese media-directory docs); de-duped triplicated design assets; built the 2027-2028 research handoff (seed + 24 packets + ingest pipeline); fixed two ReliquaryExplorer compile bugs and wrote the Anno↔PilgrimCore bridge.
---

# Handoff — 2026-08-25 (Anno app completion focus)

## TL;DR
- All three "other agent" drops are now **audited and incorporated** (prayers + icons wired into Anno data/models; ReliquaryExplorer bugs fixed + bridge written).
- The repo is clean and pushed to `origin/main` (`227bb27`).
- **App is NOT finished.** Remaining work is content/asset/cleanup, not build: fill 2027–2028 dates, replace placeholder artworks, author reliquary 3D + audio assets, and remove leftover `_infra` duplicate trees.

## Work Completed (this session chain, e7e6f5e → 227bb27)
- **Verification + 2027–2028 prep** (prior sub-session, merged): `01c5bc9`..`d04c3da`. Engine A 10/10 pass; repaired 8 corrupted VI leaves (0 U+FFFD); added `validate_vietnamese_integrity.py` + artwork `example.com` rejection; `data/seed/anno_seed_2027_2028.json` (731 days); `docs/research/HANDOFF_BRIEF.md` + 24 packets + `handoff_manifest.yaml`; `tools/ingest_2027_2028.py` with proven dry-run.
- **Docs/ vs docs/ collision** resolved: removed capital `Docs/` (128 files), kept lowercase `docs/` (`82c3703`, `faf578f`).
- **Design-asset de-dup** (`fe07b5e`): removed `visuals_infra/`, `assets_infra/`, `assets/original-renders/`, `assets/anno-icons-montage.png`, `visuals/previews/` (~11 MB); kept `assets/extracted/` + root renders.
- **Media-directory docs consolidated** (`d853843`): 7 root/`incoming_zips` copies → 4 unique in `docs/research/vietnamese-catholic-media/`.
- **Agent-drop incorporation** (`227bb27`):
  - PilgrimCore bugs fixed: `sessionIsActive`→`isSessionActive` (4 sites) in `ReliquaryInteractionModel.swift`; added undeclared `recentVisitLog` in `PilgrimGuideCoordinator.swift`.
  - `AudioNarrationTrack.audioURL(for:)` now resolves local bundle files (CDN https still wins).
  - `Sanctuary` gained `iconAssetName`; 6 prayer `.mp3`s added to `audio_narration_catalog.json` (18 tracks); 5 icon `.png`s wired (2 mapped + `sacred_icons_registry.json`).
  - `Anno/Reliquary/ReliquaryBridge.swift` written (`#if canImport(PilgrimCore)` guarded) + integration spec §4 corrected (`associated3DReliquaryId`, not `primaryRelicId`) + §6 "From-Linux Status & Next Steps".

## Work In Progress
- None committed-in-progress. All changes are on `main`, pushed. The **next body of work is finishing the app** (see below) — not started.

## Open Questions
- Does the app require **full-year 2026** coverage, or only the 182-day Jul 3–Dec 31 window currently in `anno_unified_2026.json`? (calendar engine has Jan 1–Jul 2 in `calendar_2026_2029.jsonl`.)
- Which sanctuary (if any) should the 3 unmapped icons (`icon_st_barbara`, `icon_st_columban`, `rosary_garden_icon`) attach to — or is a separate Saints feature intended?
- Is `docs_infra/` a stale fork of `docs/` or an intentional "infra" variant? Must diff before deleting.

## Least Confident
- **2026 fixture coverage.** I assert `anno_unified_2026.json` = 182 days (Jul 3–Dec 31) from earlier verification, but a naive `json.load` read this session showed 4 top-level keys (likely `{"schema_version":…,"days":[…]}`), so the exact key shape should be re-confirmed before assuming ingest tooling. `[inferred]`
- **`_infra` trees.** `docs_infra/` was found to *differ* from `docs/` earlier (not byte-identical), so it may carry unique content — do NOT bulk-delete without diffing. `[inferred]`
- **Reliquary "works":** it is logically complete with a real test suite, but I could not build it (no Xcode/Apple SDKs on Linux). The two bug fixes are correct by reading; full compile is unverified. `[inferred]`

## Do Differently
- Earlier asset de-dup only removed `assets_infra/`/`visuals_infra/`/`assets` duplicates and left the `_infra` *code/docs* duplicates (`docs_infra/`, `calendar_engine_infra.py`, `localization_infra/`, `icons_infra/`). Should have caught the whole `_infra` pattern in one pass. Next cleanup pass should finish it.

## Files Touched (this session)
- `Anno/Models/AudioNarrationTrack.swift` — local-bundle URL resolution.
- `Anno/Models/Sanctuary.swift` — `iconAssetName` field.
- `Anno/Resources/audio_narration_catalog.json` — +6 prayer tracks (18 total).
- `Anno/Resources/SacredSanctuaries/our_lady_of_la_vang.json`, `north_american_martyrs_auriesville.json` — `icon_asset_name`.
- `Anno/Resources/sacred_icons_registry.json` — NEW (5 icons).
- `Anno/Reliquary/ReliquaryBridge.swift` — NEW (canImport-guarded bridge).
- `ReliquaryExplorer/Packages/PilgrimCore/Sources/PilgrimCore/ARCore/ReliquaryInteractionModel.swift` — `isSessionActive` fix.
- `ReliquaryExplorer/Packages/PilgrimCore/Sources/PilgrimCore/Coordination/PilgrimGuideCoordinator.swift` — `recentVisitLog` fix.
- `docs/RELIQUARY_VIEWER_ANNO_INTEGRATION_SPEC.md` — corrected bridge + §6 next steps.
- (Prior: `tools/ingest_2027_2028.py`, `data/seed/anno_seed_2027_2028.json`, `docs/research/handoff/*`, de-duped asset dirs, `docs/research/vietnamese-catholic-media/*`.)

## Git Scope
- Commits: `e7e6f5e..227bb27` (this session chain; all pushed to `origin/main`).
- Changed files: see Files Touched + `git show --stat 227bb27`.
- Uncommitted: **none** (working tree clean on ichabod).

## Finishing the App — Next Steps (dates, assets, cleanup)
> Build/compile is macOS-only and out of scope here; these are the *completion* tasks.

### A. Required dates (content)
1. **2026 gap:** confirm whether Jan 1–Jul 2 2026 must be in the unified fixture; if so, run Engine B research + `normalize_fixture.py` for those ~182 days. (`anno_unified_2026.json` currently 182 days Jul 3–Dec 31.)
2. **2027–2028 (731 days, 0 researched):** 24 packets at `docs/research/handoff/2027-01.md`…`2028-12.md` + `docs/research/handoff_manifest.yaml` (all `status: todo`). Dispatch each to a third-party research agent — **no filesystem access**; the packet + `data/seed/anno_seed_2027_2028.json` are self-contained. Drop returns into `data/research_tentative/2027_2028/<chunk>.json`, then per chunk run `tools/validate_engine_b_output.py` (gate) + `tools/ingest_2027_2028.py` → goal `Anno/Resources/anno_unified_2027_2028.json` (731 days, 100% EN/VI). Mark each manifest chunk `done`.
3. After unified fixtures complete, regenerate `ios-fixtures/AnnoMockData.swift` via `tools/export_swift_fixture.py` (Python side verifiable on ichabod; Swift compile needs macOS).

### B. Required assets
1. **Artwork:** 28 `example.com` placeholder `artwork.source_url` values remain in `anno_unified_2026.json` (validator now blocks *new* ones, but legacy placeholders persist). Replace with real public-domain sacred-art URLs (draw from `Anno/Resources/ArtDossiers/art_dossiers_catalog.json`, 65 verified URLs) or null them.
2. **Reliquary 3D models:** `PilgrimCore` `ReliquaryItem` references `.usdz` assets (`CruciformReliquary.usdz`, `TextileCasket.usdz`, `SilverApostolicCasket.usdz`, `LaVangRegalia.usdz`) — **none exist in repo**. Need the binary models authored (3D tooling / macOS).
3. **Hagiography audio:** `PilgrimCore` needs `narrations` per sanctuary; only 4 demo WAVs exist. Author bilingual (EN/VI) hagiography audio + transcripts for the 72 sanctuaries (prioritize the ~12 with `associated3DReliquaryId`) so `SanctuaryCatalog.validated()` passes.
4. **Icon mapping:** 3 dropped icons (`icon_st_barbara`, `icon_st_columban`, `rosary_garden_icon`) have no sanctuary — pending a Saints feature or explicit mapping (`sacred_icons_registry.json` records them).
5. **ReliquaryExplorer `Info.plist`s** (`Apps/iOS/Info.plist`, `Apps/visionOS/Info.plist`) referenced by `project.yml` but missing — required for `xcodegen`/build.
6. **`ci.yml`** referenced by README but absent.

### C. Cleanup
1. Remove leftover `_infra` duplicate trees after diffing: `docs_infra/` (differs from `docs/` — inspect first), `calendar_2026_2029_infra.jsonl`, `calendar_engine_infra.py`, `localization_infra/`, `icons_infra/`. Keep canonical `docs/`, `data/calendar_2026_2029.jsonl`, `tools/calendar_engine.py`, `localization/`, `icons/`.
2. **Fix `AGENTS.md` line 56** false claim ("AR Reliquary & Spatial Audio Modules 100% complete and verified on Linux") → "implemented; compiles only after two bug fixes; not buildable on Linux; Xcode/macOS required."
3. Address pre-commit hook nags: `CHANGELOG.md` + `AGENTS.md` `updated:` timestamp (currently stale). Update if repo policy requires.
4. Decide Anno build system: no `Anno.xcodeproj` / `Package.swift` committed (only `ios/Package.swift`, iOS 17). A shippable app needs the Xcode manifest (macOS).
5. Reconcile duplicate audio engines: Anno `SacredSpatialAudioEngine` vs PilgrimCore `AudioHagiographyPlayer`.

### D. Reliquest integration (finishing, needs macOS/Xcode)
- Add `PilgrimCore` local package dep (iOS 26 / visionOS 26 / swift-tools 6.0), activate the inert map buttons (`SacredSiteMapView.swift` ~754, ~926 — currently only `Haptics.selection()`), bridge already written. Full list in `docs/RELIQUARY_VIEWER_ANNO_INTEGRATION_SPEC.md` §6.

## Blockers / Next
- [NEXT] **Dispatch the 24 × 2027–2028 handoff packets** to third-party research agents (self-contained; no FS access). This is the highest-leverage completion task and unblocks the 2027–2028 dataset.
- [NEXT] **Replace the 28 `example.com` placeholder artworks** in `anno_unified_2026.json` with real URLs from the art-dossiers catalog.
- [BLOCKER] Reliquary 3D `.usdz` models + hagiography audio are **binary deliverables** that cannot be produced on ichabod — they require macOS/3D tooling and are the gating assets for the AR feature.

## Context Notes
- Canonical repo is on ichabod at `~/Projects/Anno`; the Windows `Z:\Projects\Anno` is the SMB mount (writes go to ichabod). All git/Python work runs on ichabod via SSH.
- `origin` = `github.com:wowthisiseasytoremember-stack/anno.git`. Pushed through `227bb27`. **Never push force;** normal pushes are fine.
- Line-ending artifact: the SMB mount (core.autocrlf) vs ichabod git can show spurious "modified" files (CRLF↔LF). Discard with `git checkout -- .` on ichabod when needed; content is intact.
- ReliquaryExplorer is intentionally kept as a separate app/product inside the repo (user decision: AR feature to drive purchases for non-travelers).
- The `validate_engine_b_output.py` gate catches non-conformant Engine B output (e.g., null fields, missing `sources`); use it on every 2027–2028 chunk return before ingest.
